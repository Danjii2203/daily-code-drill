# Anthropic API key used for challenge generation + review
OPENAI_API_KEY=

# Vercel KV (created via Vercel dashboard: Storage -> KV -> Connect to project)
# These are auto-populated if you provision KV through Vercel; only needed
# manually for local dev against a real KV instance.
KV_REST_API_URL=
KV_REST_API_TOKEN=
