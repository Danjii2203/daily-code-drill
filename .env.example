# OpenAI API key used for challenge generation + review
OPENAI_API_KEY=

# Upstash Redis (create via Vercel dashboard: Storage -> Marketplace Database
# Providers -> Upstash -> Create). Vercel auto-populates these once connected.
KV_REST_API_URL=
KV_REST_API_TOKEN=
